{% extends 'base.html.twig' %}

{% block title %}PublicationForum{% endblock %}

{% block body %}
    <h1>PublicationForum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publication_forum.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ publication_forum.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ publication_forum.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ publication_forum.date ? publication_forum.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ publication_forum.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('publication_forum_index') }}">back to list</a>

    <a href="{{ path('publication_forum_edit', {'id': publication_forum.id}) }}">edit</a>

    {{ include('publication_forum/_delete_form.html.twig') }}
{% endblock %}
